<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ivart.makedecision">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MakeDecisionActivity" />
        <activity android:name=".Activities.AboutUsActivity" />
        <activity android:name=".Activities.MyDecisionsActivity" />
        <activity android:name=".Activities.SquareActivity" />
        <activity android:name=".Activities.DescriptionActivity"></activity>
    </application>

</manifest>